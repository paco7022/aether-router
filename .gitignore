node_modules/
.next/
.open-next/
.env.local
.env*.local
# Copias de seguridad del .env antes de cada cambio de provider: llevan TODAS
# las keys en claro y el patrón .env*.local NO las matchea (no terminan en
# .local). El repo es público — jamás deben entrar.
.env.local.bak*
.env.local.backup*
out/

.vercel
.wrangler/
tsconfig.tsbuildinfo
supabase/.temp/
preset examples/

# Training datasets / model outputs — contain real user RP content; never commit
# (the export script writes to ../training-data, outside the repo, but guard anyway)
training-data/
*.jsonl

# PC deployment runtime artifacts
logs/
.sync.lock

# Operator-only moderation records — NEVER commit (PII + CSAM accusations; repo is public)
BLACKLIST*
*blacklist*
moderation-bans*
CROSSBAN*
*crossban*
moderation-evidence/
*moderation-evidence*

# CF worker build artifact (transient base64 of bundle)
cf-worker/worker_b64.txt
